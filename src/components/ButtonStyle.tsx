import React from 'react';

const ButtonStyle = ({ btnStyle }: { btnStyle: React.CSSProperties }) => {
  return <div style={btnStyle}>This is a btn style components</div>;
};

export default ButtonStyle;
